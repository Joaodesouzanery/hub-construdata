/**
 * Licitações embutidas — usadas quando o fetch de licitacoes.json falha.
 * Atualizado automaticamente por coletor.js.
 *
 * Para atualizar com licitações reais:
 *   1. Execute `node coletor.js`
 *   2. Rode `npm run build`
 */

import type { Licitacao } from "@/types/database";

const licitacoes: Licitacao[] = [];

export default licitacoes;
